---
description: GFC x86 ImmortalWrt OEM 固件构建 — 2026-07-21 会话固化（r23 / r16 基线 / ORIG / 激活认领）
globs:
  - "**/deploy/immortalwrt/**"
  - "**/gfc-os/**"
  - "**/BUILD-FIRMWARE.md"
  - "**/FIRMWARE-BUILD-HANDOFF.md"
  - "**/gfc-packages.config"
  - "**/gfc-package-index.txt"
  - "**/gfc-initd-baseline.txt"
  - "**/setup-immortalwrt-feed.sh"
  - "**/rebuild-gfc-image.sh"
  - "**/ensure-gfc-package-index.sh"
  - "**/repair-oem-rootfs.sh"
  - "**/recover-package-install.sh"
  - "**/99-gfc-firstboot"
  - "**/98-gfc-network-ports"
  - "**/97-gfc-oem-root-password"
  - "**/configure-dropbear-ssh.sh"
  - "**/SESSION_HANDOFF*FIRMWARE*"
alwaysApply: true
---

# GFC 固件构建协议（ImmortalWrt OEM · 2026-07-21 更新）

## 权威文档（优先级从高到低）

1. `docs/SESSION_HANDOFF_2026-07_FIRMWARE_BUILD_FIXES.md` — **本批完成态、r16 基线、ORIG/openssh、激活认领、踩坑**
2. `gfc-client/deploy/immortalwrt/FIRMWARE-BUILD-HANDOFF.md` — 历史任务与累计能力
3. `gfc-client/deploy/immortalwrt/BUILD-FIRMWARE.md` — 操作手册（命令、验收）
4. `docs/VERSION_AND_RELEASE.md` §1.5 + `.cursor/rules/gfc-version-release.mdc` — **试编不升产品号**
5. `docs/SESSION_HANDOFF_2026-07_OTA_LIFECYCLE.md` — reclaim / 硬退役 / OTA（平台侧）
6. `docs/NFT_ARCHITECTURE.md` / `UNBOUND_ARCHITECTURE.md` / `SINGBOX_ARCHITECTURE.md` — 数据面（须批准）

## 当前状态摘要

- **产品当前（矩阵）：** `v2.1.0` / Makefile `gfc-client 2.1.0-r1`（Minor：一层泛域名 UDP/53 嗅探；新 OEM 须 rebuild 后 manifest 对齐）
- **上次已验证 OEM 构建（历史 2026-07-21）：** `437d990` → `gfc-build-437d990-client-1.1.0-r23-…`；manifest 当时为 `gfc-client 1.1.0-r23`
- **r16 init.d 基线：** `config/gfc-initd-baseline.txt`；ORIG 缺则 build fail
- **构建机：** Ubuntu，`/opt/gfc/{sip-proxy,immortalwrt}`，用户 **gfcbuild**，Go 1.22+，`GOFLAGS=-buildvcs=false`
- **刷盘镜像：** `*ext4*combined*efi*.img.gz`（分发用 `gfc-build-<gitsha>-*` 命名）
- **SSH：** dropbear **212**；反向 SSH 需 openssh-client/keygen
- **激活：** hard-retired → admin reclaim → **平台绑线路** → 设备再激活（reclaim  alone 不上线）

## 新会话第一步

1. 读 `SESSION_HANDOFF_2026-07_FIRMWARE_BUILD_FIXES.md` §0–§5
2. 构建机设环境并 rebuild（见下）
3. 验收 manifest + ORIG 基线；分发前在镜像目录 `sha256sum -c`
4. 刷机后验 dropbear + gfc 服务；激活问题查 agent log 与平台绑定状态

## 构建入口（必须 export GFC_REPO）

```bash
export PATH=/usr/local/go/bin:$PATH
export IMT_SRC=/opt/gfc/immortalwrt
export GFC_REPO=/opt/gfc/sip-proxy/gfc-client
export GOFLAGS=-buildvcs=false
bash "$GFC_REPO/deploy/immortalwrt/scripts/rebuild-gfc-image.sh"
cd "$IMT_SRC/bin/targets/x86/64"
grep -E '^(gfc-client|dropbear|openssh-client|uhttpd|rpcd)' *.manifest
sha256sum -c gfc-build-*.img.gz.sha256   # 须在含 img 的目录
```

Feed setup：**v4** — `feeds update -i gfc` + `feeds install -f`（禁止 plain `feeds update gfc` / `feeds install -a`）。

## 已完成（禁止当「未做」重开）

- disable default-settings / fw4 / luci-light；LuCI = base + theme + mod-admin-full + luci-app-gfc
- VGA 末位 console（`GFC_VGA_CONSOLE_LAST`）；inittab 无 tty1 respawn
- kernel ABI 恢复**不删** base-files；ORIG 同步 rc.d；staging 断言 rc.common
- r16 服务基线选包 + `build_packages` **显式 compile** + ORIG **openssh symlink**
- §1.5：试编/排错 **不 bump** PKG_RELEASE / 产品 tag；`gfc-os-v*` 仅 `GFC_PUBLISH_RELEASE=1`

## 修改前必须做的事

1. 改选包 / rebuild 脚本 / firstboot：先 **差异说明**，等用户 **「确认修改」**（用户点名文件除外）
2. 新增 `CONFIG_PACKAGE_*=y`：同步 **gfc-package-index.txt**、**ensure-gfc-package-index.sh**、**build_packages compile**、**verify_required_gfc_ipks**
3. 未获批准：只读分析，不写 feed/镜像脚本
4. 数据面：走 **no-change-without-approval**

## 绝对禁止（再踩即 bug）

### 构建 / rootfs

- 合并 GFC 选包后跑 **`make defconfig`** / **`make oldconfig`**
- ABI 恢复 **`rm -rf bin/targets/x86/64/packages`**（会抹 **base-files** / rc.common）
- 只刷新 kernel 却假定 userspace 新包已进 bin（须 **build_packages 显式 compile**）
- 禁用 **default-settings** 却假设 **dropbear/uhttpd/rpcd** 仍会进镜像
- 只验 `/usr/bin/ssh` 不验 **`/usr/libexec/ssh-openssh`**（ORIG 打包在 prepare_rootfs 之前）
- 依赖全量 **`make package/compile`** 作为默认路径（脚本默认 incremental；全量仅 `GFC_FULL_PACKAGE_COMPILE=1`）
- legacy **`package/gfc/`**；非空 **`DEPENDS`**；**`CONFIG_PACKAGE_nftables`**；**`CONFIG_PACKAGE_kmod-sched-htb`**
- ipk 装 **`/www/index.html`**；deploy `*.sh` 保持 0644
- 只同步 **root-x86** 不同步 **root.orig-x86** / rc.d
- 试编成功就 **bump PKG_RELEASE 或切 tag**（违反 §1.5）

### LuCI / 防火墙

- 启用 **`luci-light`** / **`luci`** meta / **`luci-app-firewall`** / **firewall4**

### 激活 / 生命周期（排障时）

- 把 **hard-retired** 当「线路码未存储」（看 log 是否已有 POST /activate）
- **reclaim 后**不绑线路就期望在线（须 **assign line** + 再激活）
- 未经批准改 nft/unbound/sing-box

## r16 必要包契约（`gfc-packages.config`）

| 类别 | 包 |
|------|-----|
| GFC | gfc-client, luci-app-gfc, luci-base, luci-theme-bootstrap, luci-mod-admin-full |
| SSH/远程 | dropbear, autossh, openssh-client, openssh-keygen |
| Web/LuCI | uhttpd, rpcd |
| LAN | dnsmasq-full, odhcpd-ipv6only |
| 数据面 | sing-box, unbound-daemon, nftables-json, kmod-tun, …（见 config 全文） |

**不强制：** autocore, gpio_switch, led, lm-sensors, packet_steering, sysfsutils, firewall4。

**动态 init.d：** `gfc-reverse-ssh` 由 agent 在平台开隧道时写入，不在静态基线列表。

## 允许的无须批准操作

- 只读：manifest / ORIG / `logread -e gfc-agent` / gap 对照 SESSION_HANDOFF
- 用户授权时：构建机跑 `rebuild-gfc-image.sh`
- 用户要求时：更新 SESSION_HANDOFF / BUILD-FIRMWARE / 本规则

## 固件任务固定口令

> 读 `SESSION_HANDOFF_2026-07_FIRMWARE_BUILD_FIXES.md`；`export GFC_REPO` 后 rebuild；以 manifest **gfc-client rN + dropbear/openssh** 与 **gfc-build-* img** 为准；试编不升号（§1.5）；激活：reclaim → 绑线路 → 再激活；数据面改前先 diff 等确认。

## 违规处理

文档与构建机实测（manifest、ORIG init.d、dropbear 二进制、rc.common）冲突时，以 **构建机/刷机实测** 报 bug，不得用无 SSH 或无 GFC 的镜像迁就错误实现。
