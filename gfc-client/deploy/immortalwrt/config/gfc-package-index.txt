# GFC OEM image package list (mirrors gfc-packages.config).
# Format: one OpenWrt *Package* name per line (not PROVIDES alias).
#
# core-gfc (gfc feed — never feeds install here)
gfc-client
luci-app-gfc
luci-base
#
# data-plane (base tree unless noted)
sing-box
unbound-daemon
unbound-checkconf
kmod-tun
kmod-nft-core
nftables-json
dnsmasq-full
libcap-bin
ca-bundle
ip-full
tc-tiny
kmod-sched-core
kmod-ifb
#
# remote-ops (packages feed)
autossh
#
# scene-ops-tools (packages feed)
curl
wget-ssl
tcpdump
iftop
bmon
#
# first-boot rootfs expand (base tree — resize2fs is NOT part of e2fsprogs)
resize2fs
parted
partx
