#!/usr/bin/env bash
# Backward-compatible wrapper — prefer lib-unbound-nft.sh.
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
# shellcheck source=lib-unbound-nft.sh
source "$SCRIPT_DIR/lib-unbound-nft.sh"
