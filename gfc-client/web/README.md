# Deprecated — Vue3 standalone admin UI

Management is **LuCI only**: `/cgi-bin/luci/admin/gfc`

Device activation: `http://<device-ip>/gfc/activate.html`

This directory is kept for reference; `deploy/build.sh` no longer builds it.
