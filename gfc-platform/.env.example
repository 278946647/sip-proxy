# Optional — leave unset on first boot to auto-generate and store in control-plane DB.
# View / change in Web UI: 系统设置 → 平台安全（仅管理员，需确认弹窗）
#
# GFC_BOOTSTRAP_TOKENS=gfc-auto-generated-on-first-boot
# GFC_ADMIN_DEFAULT_PASSWORD=admin123
# 默认 admin123；首次登录仍须强制修改。自定义此项则不再走固定初始密码流程。
# GFC_AUTH_SECRET=auto-generated-on-first-boot
#
# 客户端线路码 / 设备激活用的控制面 API 地址（主 + 备，支持域名与 IP）
# GFC_PUBLIC_URL=https://api.example.com:8080
# GFC_PUBLIC_URL_FALLBACK=http://192.168.1.10:8080
# 或仅填 IP（自动拼 http://IP:端口）：
# GFC_PUBLIC_IP=192.168.1.10
# GFC_PUBLIC_PORT=8080
#
# WebSSH 默认由控制平台自动生成 /data/pki/webssh_id 并经心跳下发公钥，无需手配密码。
# 仅覆盖默认密钥路径或临时使用密码时取消注释：
# GFC_REVERSE_SSH_CLIENT_SHELL_PASSWORD=your-immortalwrt-root-password
# GFC_REVERSE_SSH_CLIENT_SHELL_IDENTITY_PATH=/data/pki/webssh_id
#
# 删除客户端后反向端口冷却（秒），防止多租户端口复用串线（默认 3600）
# GFC_REVERSE_SSH_PORT_RELEASE_COOLDOWN_SECONDS=3600
