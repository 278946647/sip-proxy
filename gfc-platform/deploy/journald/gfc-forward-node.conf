# Caps journald disk use on forward nodes (fallback when services log to journald).
# Primary service logs should use /var/log/gfc-node/*.log + logrotate.
[Journal]
MaxRetentionSec=1day
SystemMaxUse=300M
SystemMaxFileSize=50M
MaxFileSec=1day
