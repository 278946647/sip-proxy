# GFC control plane — API / Web / co-located node agent file logs
# Installed by deploy/scripts/install-logrotate.sh (placeholder @LOG_DIR@)
@LOG_DIR@/*.log {
    daily
    rotate 1
    maxage 1
    size 50M
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
