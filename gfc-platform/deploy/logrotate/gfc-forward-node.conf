# GFC forward node — agent, sing-box, openvpn backbone
@LOG_DIR@/*.log {
    daily
    rotate 1
    maxage 1
    size 50M
    compress
    delaycompress
    missingok
    notifempty
    copytruncate
}
